import { MilitaryAgoPipe } from './military-ago.pipe';

describe('MilitaryAgoPipe', () => {
  it('create an instance', () => {
    const pipe = new MilitaryAgoPipe();
    expect(pipe).toBeTruthy();
  });
});
